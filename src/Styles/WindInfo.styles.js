import styled from "styled-components";

const WindInfoContainer = styled.div`
> div {
  display: flex;
  align-items: center;
  font-size: 1.6rem;
  color: var(--color-p2);
}
> div p {
  font-weight: 600;
  margin-left: 0.4rem;
}

`
export default WindInfoContainer